// import { getPage } from "next-page-tester";
// import { screen, fireEvent } from "@testing-library/react";
// import "@testing-library/jest-dom/extend-expect";

// Comment this OUT FOR NOW AS IT BREAKS MAJOR TESTS FOR SOME REASONS
// Here is the list of all expectations
// https://github.com/testing-library/jest-dom#tohavetextcontent

describe("Home page", () => {
  it("renders home page", async () => {
    // const { render } = await getPage({
    //   route: "/",
    // });
    // render();
    // expect(screen.getByText("Zadelis")).toBeInTheDocument();
    // fireEvent.click(screen.getByText('Link'));
    // await screen.findByText('Linked page');
  });
});
